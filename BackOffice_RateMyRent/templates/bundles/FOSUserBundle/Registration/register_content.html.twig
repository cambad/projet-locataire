{% trans_default_domain 'FOSUserBundle' %}
    <h1>Inscritpion</h1>
    <div class="m-5">
    {{ form_start(form, {'method': 'post', 'action': path('fos_user_registration_register'), 'attr': {'class': 'card p-4', 'style': 'width: 20rem; margin: 0 auto'}}) }}
        
        <div class="form-group mb-3">{{ form_row(form.surname) }}</div>
        <div class="form-group mb-3">{{ form_row(form.name) }}</div>
        <div class="form-group mb-3">{{ form_row(form.username) }}</div>
        <div class="form-group mb-3">{{ form_row(form.email) }}</div>
        <div class="form-group mb-3">{{ form_row(form.plainPassword) }}</div>
    
        <div>
            <input class="btn" type="submit" value="{{ 'Inscription'|trans }}" />
        </div>
    </div>
    {{ form_end(form) }}
